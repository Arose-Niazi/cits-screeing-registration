extends layout

block content
	div(style='text-align: center;')
		if ID
			if Arrived 
				h1(style='color:red') USER ALREADY IN VENUE
			h1 #{ID}
			h1 #{fName} #{lName}
			br
			img(src="Images/QR/"+ID+".png", alt="Not found")
		else 
			h1(style='color:red') NOT REGISTERED
